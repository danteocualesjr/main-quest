# Optional: enable richer AI responses via OpenAI (falls back to local matching without it)
# OPENAI_API_KEY=sk-...

# Optional: canonical URL for Open Graph and exported share text (defaults to https://main-quest.app)
# NEXT_PUBLIC_SITE_URL=https://your-domain.vercel.app
